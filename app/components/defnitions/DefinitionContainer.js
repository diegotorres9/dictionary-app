export default function DefinitionContainer ({data}) {
    return (
        <section>
        {/* {data && <pre>{JSON.stringify(data, null, 2)}</pre>} */}
        <p>RESULTS HEEREE</p>
        {console.log(data)}
        </section>
    )
}